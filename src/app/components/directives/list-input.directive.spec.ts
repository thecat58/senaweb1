import { ListInputDirective } from './list-input.directive';

describe('ListInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ListInputDirective();
    expect(directive).toBeTruthy();
  });
});
